name: E2E CI Governance

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main ]
  schedule:
    - cron: '0 9 * * *'
  workflow_dispatch:

jobs:
  pre-flight:
    name: 🛡️ Pre-flight Validation
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
      
      - name: Contract Violation Detector
        run: |
          chmod +x ./scripts/ci-contract-check.sh
          ./scripts/ci-contract-check.sh
      
      - name: Install Dependencies
        run: npm ci
      
      - name: Lint Contract Enforcement
        run: |
          npm run lint || {
            echo "❌ LINT CONTRACT VIOLATION: Code does not pass linting."
            exit 1
          }
      
      - name: Type Contract Enforcement
        run: |
          npx tsc --noEmit || {
            echo "❌ TYPE CONTRACT VIOLATION: TypeScript compilation errors detected."
            exit 1
          }
      
      - name: Source Integrity Check
        run: |
          chmod +x ./scripts/ci-preflight.sh
          ./scripts/ci-preflight.sh

  build:
    name: 🏗️ Global Build Contract
    needs: pre-flight
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
      
      - name: Install Dependencies
        run: npm ci
      
      - name: Build Contract Enforcement
        run: |
          npm run build || {
            echo "❌ BUILD CONTRACT VIOLATION: 'npm run build' failed."
            echo "   E2E tests CANNOT run until build succeeds."
            exit 1
          }
          
          if [ ! -d ".next" ]; then
            echo "❌ BUILD CONTRACT VIOLATION: .next directory not created."
            echo "   Build appeared to succeed but output is missing."
            exit 1
          fi
          
          echo "✅ Build contract satisfied. Artifact created."
      
      - name: Package Build Artifacts
        uses: actions/upload-artifact@v4
        with:
          name: next-build
          path: |
            .next/
            public/
            package.json
            next.config.mjs
          retention-days: 1

  test:
    name: 🧪 E2E Suite (${{ matrix.project }})
    needs: build
    runs-on: ubuntu-latest
    timeout-minutes: 15
    strategy:
      fail-fast: false
      matrix:
        project: [chromium, mobile-chrome, tablet]
    
    steps:
      - uses: actions/checkout@v4
      
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
      
      - name: Restore Build Artifacts
        uses: actions/download-artifact@v4
        with:
          name: next-build

      - name: Install Runtime Dependencies
        run: |
          npm ci --omit=dev
          npm install @playwright/test @types/node tsx
      
      - name: Playwright Browser Contract
        run: |
          npx playwright install --with-deps ${{ matrix.project }} || {
            echo "❌ BROWSER CONTRACT VIOLATION: Failed to install Playwright browsers."
            exit 1
          }
      
      - name: Start Application (Isolation Contract)
        run: |
          # Enforce PORT 3001 for E2E isolation
          PORT=3001 npx next start &
          SERVER_PID=$!
          echo "SERVER_PID=$SERVER_PID" >> $GITHUB_ENV
          
          # Verify process started
          sleep 2
          if ! kill -0 $SERVER_PID 2>/dev/null; then
            echo "❌ ISOLATION CONTRACT VIOLATION: Server process failed to start."
            exit 1
          fi
          
          echo "✅ Server started (PID: $SERVER_PID)"
        env:
          NODE_ENV: production
          PORT: 3001

      - name: Readiness Contract Enforcement
        run: |
          chmod +x ./scripts/wait-for-app.sh
          ./scripts/wait-for-app.sh http://localhost:3001/api/health 120 || {
            echo "❌ READINESS CONTRACT VIOLATION: Application did not signal readiness."
            echo "   /api/health must respond within 120 seconds."
            exit 1
          }

      - name: Mandatory Guardrail Check
        run: |
          READY_SIGNAL=$(curl -s http://localhost:3001/api/health)
          
          if [[ ! "$READY_SIGNAL" == *"\"ready\":true"* ]]; then
            echo "❌ READINESS CONTRACT VIOLATION: Invalid health response."
            echo "   Expected: {\"ready\": true}"
            echo "   Received: $READY_SIGNAL"
            exit 1
          fi
          
          echo "✅ Guardrail passed. Application ready for E2E tests."

      - name: Determinism Contract Check
        run: |
          # Verify no external dependencies are reachable unless mocked
          echo "✅ Determinism: All external calls should use local mocks or fixtures."
          # Add specific checks here if needed (e.g., network isolation)

      - name: Run Complete E2E Suite
        run: |
          npx playwright test --project=${{ matrix.project }} || {
            echo "❌ E2E TESTS FAILED for ${{ matrix.project }}"
            exit 1
          }
        env:
          CI: true
          PORT: 3001

      - name: Cleanup Contract Enforcement
        if: always()
        run: |
          echo "🛑 Enforcing cleanup contract..."
          
          if [ -n "$SERVER_PID" ]; then
            echo "Stopping server (PID: $SERVER_PID)"
            kill $SERVER_PID 2>/dev/null || true
            
            # Verify process is dead
            sleep 1
            if kill -0 $SERVER_PID 2>/dev/null; then
              echo "⚠️  Process still alive, forcing kill..."
              kill -9 $SERVER_PID 2>/dev/null || true
            fi
          fi
          
          # Fallback: sweep port 3001
          npx kill-port 3001 || true
          
          # Verify port is free
          if lsof -ti:3001 >/dev/null 2>&1; then
            echo "❌ CLEANUP CONTRACT VIOLATION: Port 3001 still occupied."
            lsof -ti:3001 | xargs kill -9 || true
          fi
          
          echo "✅ Cleanup complete. No orphaned processes."

      - name: Upload Test Evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: evidence-${{ matrix.project }}
          path: |
            test-results/
            playwright-report/
          retention-days: 7

  governance-report:
    name: 📊 CI Governance Summary
    needs: [pre-flight, build, test]
    if: always()
    runs-on: ubuntu-latest
    steps:
      - name: Evaluate Pipeline Conclusion
        uses: actions/github-script@v7
        with:
          script: |
            const results = {
              'Pre-flight': '${{ needs.pre-flight.result }}',
              'Build': '${{ needs.build.result }}',
              'E2E Tests': '${{ needs.test.result }}'
            };
            
            console.log('━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━');
            console.log('CI GOVERNANCE REPORT');
            console.log('━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━');
            console.table(results);
            
            const allPassed = Object.values(results).every(r => r === 'success');
            const anyFailed = Object.values(results).some(r => r === 'failure');
            
            if (anyFailed) {
              const failedJobs = Object.entries(results)
                .filter(([_, status]) => status === 'failure')
                .map(([job, _]) => job);
              
              core.setFailed(
                `CI Governance: Contract violations detected in: ${failedJobs.join(', ')}`
              );
            } else if (allPassed) {
              console.log('✅ All CI contracts satisfied.');
            } else {
              console.log('⚠️  Some jobs were skipped or cancelled.');
            }
            
            const summary = `
            # CI Governance Report
            
            **Status**: ${allPassed ? '✅ PASS' : '❌ FAIL'}
            
            ## Contract Enforcement
            
            | Contract | Status |
            |----------|--------|
            | Pre-flight Validation | ${results['Pre-flight'] === 'success' ? '✅' : '❌'} |
            | Build Contract | ${results['Build'] === 'success' ? '✅' : '❌'} |
            | E2E Test Execution | ${results['E2E Tests'] === 'success' ? '✅' : '❌'} |
            
            ${anyFailed ? '⚠️ **Action Required**: Review failed jobs above.' : ''}
            `;
            
            core.summary.addRaw(summary).write();
